export enum ListingStatus {
  OPEN = 'OPEN',
  MATCHED = 'MATCHED',
  COMPLETED = 'COMPLETED',
  EXPIRED = 'EXPIRED',
}

export enum RecipientType {
  STORE = 'STORE',
  FACILITY = 'FACILITY',
}

export enum UserRole {
  STORE = 'STORE',
  FACILITY = 'FACILITY',
  ADMIN = 'ADMIN',
}
